# Brand assets

No MSCI Mobilindo logo file has been provided yet — the 20 photos uploaded on
2026-08-13 were all individual vehicle photos, not a logo, katalog page, or
formulir scan. Navbar/Footer continue to use the text wordmark until a real
logo file lands here.

Expected filename once available: `logo.png` / `logo.svg` (whichever format
is provided) — update `components/layout/Navbar.tsx` and
`components/layout/Footer.tsx` to render it in place of the text wordmark,
and `app/icon.tsx` if it's suitable as a favicon source.
