/**
 * The business's own Google Maps share link, supplied directly rather
 * than derived from a search query. This is the single Maps CTA URL used
 * everywhere on the site (floating button, Location/Contact section) —
 * the older text-search and embed-URL approaches are no longer used.
 */
export function getGoogleMapsLink(): string {
  return "https://share.google/GckJ8epEkuc9l4n0I";
}
